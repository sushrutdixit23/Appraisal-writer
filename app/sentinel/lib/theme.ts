// Sentinel — Black + Sandstone identity, scoped to /sentinel routes only.
// Institutional research-product look (McKinsey report / Economist /
// Goldman deck): hairline borders instead of shadows, near-square corners,
// no monospace, serif display headings, exactly two chart hues.

export const T = {
  background: "#F7F3EB",
  card: "#FFFFFF",
  ink: "#161616",
  inkSoft: "#6B6660",
  sidebar: "#111111",
  sidebarText: "#EDE7DC",
  sidebarTextMuted: "#8C887F",
  accent: "#A47551",
  accentSoft: "#EFE6DB",
  rule: "#DFD7C8",
  chartPrimary: "#161616",
  chartAccent: "#A47551",
} as const;

export const SERIF = `"Source Serif 4", Georgia, "Times New Roman", serif`;
export const SANS = `"IBM Plex Sans", -apple-system, "Segoe UI", sans-serif`;
